export type FormatMeta = {
  id: string;
  label: string;
  description?: string;
  enabled: boolean;
};

export type TemplateItem = {
  id: string;
  label: string;
  kind: string;
  enabled: boolean;
};
